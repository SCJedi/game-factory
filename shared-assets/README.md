# Shared Assets

Place reusable assets here - sprites, sound files, fonts, tilesets - that multiple games can reference.

Games can import from this directory using a relative path like `../../shared-assets/sprite.png` or by symlinking into their `public/` folder.

This directory is empty by default. The first game (Pulse Dodge) uses only procedural drawing and Web Audio, so no shared assets were needed.
